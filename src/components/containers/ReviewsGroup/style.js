import styled from 'styled-components';

import { mainColors } from '../../../styles/variables';

const { baseBorderColor } = mainColors;

export const ReviewsIndicator = styled.p`
  font-size: 1rem;
  text-transform: uppercase;
  color: ${baseBorderColor};
  font-weight: bold;
`;

